#!/bin/bash
# Author: Pranav Sivvam

FILE="OVPN location"
OUT_FILE="$HOME/.htbtun_out"

openvpn $FILE > $OUT_FILE &
sleep 8
BOOL=$(grep -i 'Initialization Sequence Completed' $OUT_FILE)
#echo $BOOL
if [[ ! $BOOL ]]; then
    echo "Could not connect to HTB"
    echo "Log file: $OUT_FILE"
else
    echo "Connected"
fi
